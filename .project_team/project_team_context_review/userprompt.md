# User Prompt

Address GitHub issues #27 and #28 in claudechic, expanded into a broader review:

- **#27** -- Allow workflow phases to suppress identity.md injection for standing-by agents.
- **#28** -- Make the injected `## Constraints` block configurable via settings.

The user expanded scope: tighten the `project_team` workflow by reviewing the time, place, and role of context delivery to its agents, so the team has what it needs to drive the project forward at every step. Use the most recent `project_team` session (`abast_accf332_sync`) as a source of observed failure modes to ground the review.

Vision approved (v4).
