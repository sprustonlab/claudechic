# Graduation

Congratulations! You've experienced:
- Workflow activation and deactivation (`/tutorial`, `/tutorial stop`)
- Phase transitions with advance_checks
- deny, warn, and log enforcement levels
- Phase-scoped rules
- Injections that modify tool input
- Hints with different lifecycles
- Hit logging to `.claude/hits.jsonl`
- Global rules vs workflow-scoped rules

Run `/tutorial stop` to deactivate the tutorial workflow.
